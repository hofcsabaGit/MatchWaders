// enum structs for States and states
public class States {
	// States
	public enum EnemyMovementState { LeftBorder, RightBorder, Moving }
	public enum GameState { MainMenu, InGame, Paused, WinGame, LoseGame }

	// TODO Separate
	// ran out of time..

	// Type "States"
	public enum BulletType { EnemyBullet, PlayerBullet }
	public enum EnemyType { Red, Green, Blue, Yellow }
}
